<Window x:Class="InterlaboratoryUNIIM.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:InterlaboratoryUNIIM"
        xmlns:vm="clr-namespace:InterlaboratoryUNIIM.ViewModel"
        mc:Ignorable="d"
        Title="{Binding Title}" Height="650" Width="800">
    <Window.DataContext>
        <vm:MainWindowVM/>
    </Window.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0">
            <Label Content="Стандартное отклонение"/>
            <TextBox Text="{Binding StandardDeviation}"/>
            <DataGrid ItemsSource="{Binding DataSet}" AutoGenerateColumns="False" IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Delta" Binding="{ Binding DataDelta}" Width="*"/>
                    <DataGridTextColumn Header="SD" Binding="{ Binding DataStandardDeviation}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
            <Button Content="СКО" Command="{Binding TestMeanCommand}"/>
            <Label Content="{Binding TestSKO}"/>
            <Label Content="{Binding Duration}"/>
        </StackPanel>
        <StackPanel Grid.Column="1">
            <StackPanel Orientation="Horizontal">
                <Label Content="MEAN"/>
                <TextBox Text="{Binding TestSKO}" Width="200"/>
                <Button Content="Расчитать"  Width="100"/>
            </StackPanel>
            
        </StackPanel>
    </Grid>
</Window>
